{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "majoor.omnicam.shot.v1",
  "title": "Director shot",
  "description": "One camera plus its optional media, as built by omnicam/core/director_shot.py.",
  "type": "object",
  "required": ["schema_version", "kind", "id", "name", "video", "audio", "camera_track", "metadata"],
  "properties": {
    "schema_version": {"const": 1},
    "kind": {"const": "omnicam_shot"},
    "id": {"type": "string", "minLength": 1, "maxLength": 4096},
    "name": {"type": "string", "minLength": 1, "maxLength": 4096},
    "video": {"description": "A runtime VIDEO object or null; not serialisable, so left untyped."},
    "audio": {"description": "A runtime AUDIO object or null; not serialisable, so left untyped."},
    "camera_track": {"$ref": "majoor.omnicam.track.v1"},
    "metadata": {"type": "object"}
  },
  "additionalProperties": false
}
